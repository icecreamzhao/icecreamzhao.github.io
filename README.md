# 说明

本项目使用的是 hugo 作为博客框架，由于使用简单的原因，可以让我把注意力更聚焦于博客内容而并非样式或者搭建环境上。

# hugo 相关

```shell
# 本地启动服务并查看
hugo server

# 新增一篇博客
hugo new posts first-post.md
```

# 样式

`layouts_custom_backup` 文件夹中存放的是自定义样式，但是现在目前使用的是 `paperMod` 样式，如果想更改成自定义样式，需要将 `layouts_custom_backup` 文件夹重新命名为 `layouts` 文件夹。
